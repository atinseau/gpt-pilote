{
  "name": "@gpt-pilote/extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tsc -watch",
    "build": "tsc",
    "clean": "rm -rf build",
    "lint": "eslint --ext .ts src",
    "check-types": "tsc --noEmit",
    "fclean": "pnpm clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "@gpt-pilote/eslint-config-custom": "workspace:*",
    "@types/node": "16.x",
    "@types/vscode": "^1.76.0",
    "@vscode/test-electron": "^2.2.3",
    "typescript": "^4.9.5"
  }
}